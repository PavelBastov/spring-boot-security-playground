spring.application.name=spring-security-playground

# Verbose Spring Security logging — shows auth, authorization decisions,
# and the per-request filter chain. Dial back to DEBUG or remove when done.
# logging.level.org.springframework.security=TRACE